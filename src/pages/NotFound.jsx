import React from 'react';

const NotFound = () => {
    return (
        <div>
             No encontrado, error 404. {/* hacer animación o algo diferente, que le de vida y mejor aspecto */}
        </div>
    );
};

export default NotFound;